# LanceDB 多维聚簇功能 - 设计文档 v1.0

**版本**: v1.0  
**日期**: 2026-04-11  
**状态**: 已确认，待实现

---

## 1. 设计目标与原则

### 1.1 核心目标

为 LanceDB 引入多维聚簇能力，通过按指定列排序数据，优化范围查询性能。

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| **简洁性** | 功能聚焦，接口简单，避免过度设计 |
| **渐进式** | V1 仅支持核心功能，高级特性后续迭代 |
| **零侵入** | 不修改 Lance 核心，利用现有扩展机制 |
| **透明性** | 聚簇是纯物理优化，不影响数据语义 |

### 1.3 版本范围

**V1 实现的功能**:
- 建表时指定 1-4 个聚簇键
- 全局重写（`full: true`）
- 自动重建索引
- 利用现有 min-max 统计优化查询

**V1 不实现的功能**:
- 增量聚簇（预留接口，调用时报错）
- 聚簇键变更
- 聚簇效果监控
- 复杂的自适应归一化

---

## 2. 整体架构

### 2.1 架构概览

聚簇功能采用**分层架构**：

- **接口层**: 用户通过 `Table` 和 `CreateTableBuilder` API 与聚簇功能交互
- **配置层**: 管理聚簇配置的读取、验证和存储
- **算法层**: 实现具体的排序算法（Hilbert 曲线、直接排序）
- **执行层**: 处理全局排序、数据重写和索引重建

### 2.2 模块划分

聚簇功能封装在独立的 `cluster` 模块中，但不对用户公开。用户仅通过 `Table` 提供的公开方法访问聚簇能力。

**模块可见性**:

| 模块 | 可见性 | 说明 |
|------|--------|------|
| `cluster` | 私有 | 内部实现，不导出到 `lib.rs` |
| `Table::cluster_config` | 公开 | 用户查询当前聚簇配置 |
| `Table::optimize` | 公开 | 通过 `OptimizeAction::Cluster` 执行聚簇 |
| `CreateTableBuilder::cluster_by` | 公开 | 建表时设置聚簇键 |

### 2.3 数据流

用户执行聚簇操作的数据流：

1. **调用阶段**: 用户调用 `table.optimize(OptimizeAction::Cluster { full: true })`
2. **配置读取**: 系统从表元数据中读取聚簇配置（聚簇键、算法）
3. **排序准备**: 根据聚簇键数量和类型，选择合适的算法
4. **数据排序**: 读取全表数据，按聚簇键计算排序键，全局排序
5. **数据写入**: 按目标行数分批写入新的 fragments
6. **索引重建**: 删除旧索引，基于新 fragments 重建索引
7. **事务提交**: 原子性切换到新的 fragments 和索引

如果任何步骤失败，临时写入的数据会被清理，表保持原有状态。

---

## 3. 配置管理

### 3.1 配置内容

聚簇配置包含以下信息：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `keys` | 字符串数组 | 是 | 聚簇键列名，1-4 个 |
| `algorithm` | 字符串 | 否 | 算法名称，默认 "hilbert" |
| `algorithm_params` | JSON 对象 | 否 | 算法参数，如 `{"bits": 16}` |

**算法选择规则**:
- 1 维：自动使用 "direct"（直接排序）
- 2-4 维：使用 "hilbert"（Hilbert 曲线）

### 3.2 配置存储

聚簇配置以 JSON 形式存储在表的 `schema_metadata` 中，键名为 `lancedb.cluster.config`。

**存储位置选择**:

| 存储位置 | 优点 | 缺点 | 决策 |
|----------|------|------|------|
| Schema Metadata | 与表绑定，版本化 | 大小有限 | **选用** |
| 独立文件 | 灵活 | 需额外同步 | 不选用 |
| Manifest 扩展 | 官方支持 | 需 Lance 修改 | 不选用 |

**键名设计决策**:
- 采用**单键存储** `lancedb.cluster.config`，而非分散存储多个键
- 理由：
  1. **内聚性**：聚簇配置是整体概念，JSON 存储更符合逻辑
  2. **简洁性**：减少 schema_metadata 键数量，避免分散管理
  3. **可扩展性**：后续添加配置项只需修改结构体，无需新键
  4. **一致性**：类似 LanceDB 其他功能（如 embedding 定义）的存储方式

### 3.3 配置验证

建表时验证聚簇配置的合法性：

| 验证项 | 规则 | 错误提示 |
|--------|------|----------|
| 重复列名 | 聚簇键不可重复 | "Duplicate clustering key column: {col}" |
| 空列名 | 列名不可为空 | "Empty clustering key column name" |
| 列存在性 | 列必须存在于 schema | "Clustering key column '{col}' not found" |
| 类型支持 | 仅支持数值和时间类型 | "Type '{type}' is not supported" |
| 维度数量 | 1-4 个聚簇键 | "Clustering keys count {n} exceeds maximum of 4" |

### 3.4 配置的不可变性

V1 中聚簇键建表后**不可变更**。预留 `set_cluster_keys` 接口，但调用时返回 `NotSupported` 错误。

---

## 4. 聚簇算法

### 4.1 算法选择

根据聚簇键数量自动选择算法：

| 维度数 | 算法 | 核心思想 |
|--------|------|----------|
| 1 | Direct Sort | 按单列值直接排序 |
| 2-4 | Hilbert Curve | 多维空间填充曲线，保持局部性 |

### 4.2 Hilbert 曲线算法

**算法原理**:

Hilbert 曲线是一种空间填充曲线，将多维空间中的点映射到一维空间，同时尽量保持空间局部性（相近的多维点在一维上也有较近距离）。

**归一化策略**:

将各聚簇键的值线性映射到统一整数范围 `[0, 2^bits-1]`（默认 16 位，即 0-65535），然后计算 Hilbert 索引。

**范围差异问题**:

不同列的数值范围差异会影响聚簇效果。例如：
- 年龄列范围 0-100
- 收入列范围 0-1,000,000

归一化后，年龄变化 1 岁对应约 655 个单位，收入变化 1 元对应约 0.065 个单位。这导致年龄列主导聚簇顺序。

**应对策略**:

V1 采用**简单线性归一化**，通过文档指导用户：
1. 选择数值范围相近的列作为聚簇键
2. 或预处理数据（如取对数、分桶）后再聚簇

参考友商（Databricks、ClickHouse）的做法，不自动进行复杂的范围调整。

### 4.3 直接排序算法

单维度时使用 Arrow 内置排序功能，按列值升序排列。

NULL 值处理：放在末尾（或单独处理）。

### 4.4 算法扩展性

通过 `ClusteringAlgorithm` trait 支持后续添加新算法：

- 预留 trait 接口，新算法实现后注册到算法注册表
- V1 不公开给用户自定义算法（内部使用）

---

## 5. 用户接口设计

### 5.1 建表接口

创建表时指定聚簇配置：

| 方法 | 参数 | 说明 | 示例 |
|------|------|------|------|
| `cluster_by` | 列名数组 | 设置聚簇键 | `.cluster_by(&["timestamp", "category"])` |
| `cluster_algorithm` | 算法名 | 可选，默认自动选择 | `.cluster_algorithm("hilbert")` |

**链式调用示例**:
```
db.create_table("my_table", data)
  .cluster_by(&["col1", "col2"])
  .cluster_algorithm("hilbert")  // 可选
  .execute()
```

### 5.2 查询接口

获取当前表的聚簇配置：

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `cluster_config` | `Option<ClusteringConfig>` | 返回配置或 None（未配置） |

返回的 `ClusteringConfig` 包含字段：`keys`（键列表）、`algorithm`（算法名）、`algorithm_params`（参数）。

### 5.3 执行接口

通过 Optimize 系统执行聚簇：

**OptimizeAction::Cluster 参数**:

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `full` | bool | true | true: 全局重写；false: 预留，调用报错 |
| `target_rows_per_fragment` | Option<usize> | None | 每 fragment 目标行数 |

**使用示例**:
```
// 全局重写（实际功能）
table.optimize(Cluster { full: true, target_rows_per_fragment: Some(100000) })

// 增量重写（预留，会报错）
table.optimize(Cluster { full: false, ... })  // Error: NotImplemented
```

---

## 6. 执行流程设计

### 6.1 执行阶段

全局重写（`full: true`）的执行分为以下阶段：

| 阶段 | 操作 | 失败处理 |
|------|------|----------|
| 1. 配置读取 | 从 schema_metadata 读取聚簇配置 | 报错（未配置则提示） |
| 2. 算法选择 | 根据维度数选择算法 | 报错（未知算法） |
| 3. 数据扫描 | 创建全表数据流 | 报错 |
| 4. 数据排序 | 计算排序键，全局排序 | 报错 |
| 5. 数据写入 | 分批写入临时 fragments | 清理临时数据，报错 |
| 6. 索引重建 | 删除旧索引，重建新索引 | 清理临时数据，报错 |
| 7. 事务提交 | 原子切换到新 fragments | 清理临时数据，报错 |

### 6.2 流式处理

为支持大数据集，采用流式处理：

- **读取**: 分批读取数据，避免全量加载到内存
- **排序**: 使用外部排序（如需要），或基于 DataFusion 的排序执行计划
- **写入**: 边排序边写入，达到 `target_rows_per_fragment` 即创建新 fragment

### 6.3 索引重建

聚簇会改变数据的物理布局（row_ids 变化），因此必须重建索引：

**重建流程**:
1. 记录当前所有索引的配置（列、类型、参数）
2. 删除旧索引文件
3. 使用原始配置，基于新的 fragments 重建索引
4. 如果重建失败，整个聚簇操作失败

**注意事项**:
- 重建索引可能耗时较长，与索引数量和数据量成正比
- V1 不支持跳过索引重建
- 不支持选择性地重建部分索引

### 6.4 事务与回滚

利用 Lance 的事务机制保证原子性：

- **临时数据**: 新 fragments 写入时标记为临时，提交后才可见
- **失败清理**: 任何阶段失败时，清理已写入的临时 fragments 和索引
- **并发控制**: 遵循 Lance 的并发规则，聚簇期间禁止其他写入

---

## 7. 查询优化

### 7.1 优化方式

聚簇后的查询优化**不依赖额外的聚簇元信息**，而是利用 Lance 已有的 min-max 统计：

**原理**:
- 数据按聚簇键有序存储
- Lance 自动维护每列的 min/max 统计
- 查询时根据 WHERE 条件的范围，跳过不可能包含匹配值的 fragments

### 7.2 效果预期

| 场景 | 效果 |
|------|------|
| 刚完成 Cluster | 数据完全有序，min-max 过滤效果最佳 |
| 继续写入后 | 新数据无序，整体过滤效果下降 |
| 多次 Compact | 可能破坏原有顺序，效果下降 |

### 7.3 用户责任

由于不维护 fragment 级聚簇状态，用户需要：

1. **感知数据状态**: 知道何时数据可能乱序
2. **主动维护**: 根据需要重新执行 `Cluster { full: true }`
3. **查询配合**: 在 WHERE 条件中包含聚簇键，才能触发 min-max 过滤

---

## 8. 跨语言绑定

### 8.1 Python 绑定

**CreateTableBuilder 扩展**:

| 方法 | 参数 | 说明 |
|------|------|------|
| `cluster_by(columns)` | List[str] | 设置聚簇键 |
| `cluster_algorithm(algorithm)` | str | 设置算法（可选） |

**Table 扩展**:

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `cluster_config()` | dict \| None | 获取配置 |

**OptimizeAction 扩展**:

```python
# 全局重写
Optimize.cluster(full=True, target_rows_per_fragment=100000)

# 增量（报错）
Optimize.cluster(full=False)  # NotImplementedError
```

### 8.2 Node.js 绑定

**CreateTableOptions 扩展**:

| 属性 | 类型 | 说明 |
|------|------|------|
| `clusterBy` | string[] | 聚簇键数组 |
| `clusterAlgorithm` | string | 算法名（可选） |

**Table 扩展**:

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `clusterConfig()` | Promise<ClusterConfig \| null> | 获取配置 |

**Optimize 扩展**:

```typescript
Optimize.cluster({ full?: boolean, targetRowsPerFragment?: number })
```

---

## 9. 错误处理

### 9.1 错误类型

| 错误场景 | 错误类型 | 说明 |
|----------|----------|------|
| 配置验证失败 | InvalidInput | 参数不合法 |
| 表未配置聚簇 | InvalidInput | 调用 Cluster 但未设置聚簇键 |
| 增量聚簇 | NotImplemented | `full: false` 不支持 |
| 变更聚簇键 | NotSupported | `set_cluster_keys` 不支持 |
| 算法未知 | InvalidInput | 指定的算法不存在 |
| 执行失败 | Runtime | 排序、写入、索引重建失败 |

### 9.2 错误恢复

所有错误都应提供清晰的错误信息，帮助用户定位问题。

执行阶段的错误（Runtime）需要确保：
- 不产生部分结果
- 不遗留临时文件
- 表保持调用前状态

---

## 10. 测试策略

### 10.1 单元测试

覆盖核心逻辑的正确性：

- **配置验证**: 各种合法和非法配置的验证结果
- **算法正确性**: 排序结果是否符合预期（局部性、顺序）
- **序列化**: 配置的序列化和反序列化

### 10.2 集成测试

覆盖端到端流程：

- **建表配置**: 正常和异常场景
- **全局重写**: 数据正确性、fragment 数量、索引重建
- **查询验证**: 聚簇后查询结果正确，性能提升（可选基准）
- **失败回滚**: 模拟各阶段失败，验证清理和状态恢复

### 10.3 边界测试

- 空表聚簇
- 全 NULL 值聚簇
- 单 fragment 聚簇
- 超大表聚簇（流式验证）
- 并发聚簇（冲突处理）

---

## 11. 文档与用户指南

### 11.1 关键文档

- **API 文档**: 自动生成，包含参数说明和示例
- **用户指南**: 聚簇键选择、使用方法、注意事项
- **最佳实践**: 如何选择聚簇键、何时重新聚簇

### 11.2 需要强调的限制

1. **数值范围差异**: 各聚簇键列的范围差异会影响效果
2. **全局重写成本**: Cluster 操作会重写全表，注意执行时机
3. **与 Compact 的关系**: Compact 可能破坏顺序，需要时重新 Cluster
4. **增量不支持**: V1 仅支持全局重写
5. **不可变更**: 聚簇键建表后不能修改

---

## 12. 实现阶段（模块化/原子化）

### 阶段划分原则

- **垂直切片**：每个阶段都有完整的用户价值（能创建表→能聚簇→能验证）
- **先易后难**：先实现1维排序（最简单），再扩展多维
- **快速验证**：每个阶段结束都有可运行的Demo
- **前置绑定**：尽早暴露跨语言问题

---

### Phase 0: 最小可验证版本(MVP) - 3-4天

**目标**：跑通最简单的1维聚簇全流程

**范围**：
- 模块结构（`cluster/mod.rs` 空壳）
- 配置管理（读写 schema_metadata，验证逻辑）
- **仅1维直接排序**（Hilbert延后到Phase 3）
- **仅Rust API**（绑定延后）
- **简化执行**：全局排序重写，**不做索引重建**（Phase 2再做）
- 基础集成测试：验证数据正确排序

**验证标准**：
```rust
let table = db.create_table("t", data)
    .cluster_by(&["id"])
    .execute().await?;

table.optimize(Cluster { full: true, .. }).await?;
// 验证：数据按 id 有序存储
```

**风险点**：
- 配置存储/读取机制
- 与 Lance Dataset 的集成

---

### Phase 1: 健壮性 - 2-3天

**目标**：让核心流程健壮、可测试

**范围**：
- 完整的错误处理（配置验证、执行错误）
- 事务回滚机制（临时文件清理）
- 流式处理（大数据集支持，不OOM）
- 完善的单元测试（配置验证、排序正确性）

**验证标准**：
- 错误输入有清晰错误信息
- 大表（1000万行）能流式处理
- 失败时无残留临时文件

**风险点**：
- 大表流式排序的性能
- 异常情况的资源清理

---

### Phase 2: 索引重建 - 2天

**目标**：解决索引失效问题

**范围**：
- 记录当前索引配置
- 删除旧索引
- 重建索引
- 回滚时清理索引

**验证标准**：
- Cluster 后原有索引能正常使用
- 索引重建失败时整个Cluster回滚

**风险点**：
- 索引重建耗时较长（大表）
- 索引类型兼容性

---

### Phase 3: Hilbert算法 - 2-3天

**目标**：支持多维聚簇

**范围**：
- 集成 `hilbert_index` crate
- 实现多维归一化（简单线性映射）
- 算法注册表
- 自动根据维度选择算法（1维→direct，2-4维→hilbert）

**验证标准**：
- 2-4维数据能正确聚簇
- 相近数据物理存储相近
- 算法自动选择正确

**风险点**：
- Hilbert曲线库的集成问题
- 浮点数/时间类型的归一化

---

### Phase 4: Python绑定 - 2天

**目标**：Python用户可用

**范围**：
- `CreateTableBuilder.cluster_by()`
- `Table.cluster_config()`
- `OptimizeAction.cluster()`
- Python集成测试

**验证标准**：
```python
table = db.create_table("t", data).cluster_by(["x", "y"]).execute()
table.optimize(Optimize.cluster(full=True))
assert table.cluster_config()["keys"] == ["x", "y"]
```

**风险点**：
- PyO3绑定复杂性
- 类型转换问题

---

### Phase 5: Node.js绑定 - 2天

**目标**：Node.js用户可用

**范围**：
- TypeScript接口定义
- NAPI绑定实现
- Node.js集成测试

**验证标准**：
```typescript
const table = await db.createTable("t", data, { clusterBy: ["x", "y"] });
await table.optimize(Optimize.cluster({ full: true }));
```

---

### Phase 6: 完善与文档 - 2-3天

**目标**：产品化

**范围**：
- 性能基准测试
- 用户文档（聚簇键选择指南）
- API文档完善
- 边界情况测试（空表、全NULL、并发等）

**验证标准**：
- 有完整的使用文档
- 有性能对比数据
- 边界情况处理正确

---

## 阶段依赖关系

```
Phase 0 (MVP-1D)
    │
    ▼
Phase 1 (健壮性)
    │
    ▼
Phase 2 (索引重建)
    │
    ▼
Phase 3 (Hilbert-多维)
    │
    ├──▶ Phase 4 (Python) ──▶ Phase 5 (Node.js)
    │
    ▼
Phase 6 (完善)
```

**可并行**：Phase 4和Phase 5在Phase 3完成后可部分并行

---

## 工期汇总

| Phase | 工期 | 累计 |
|-------|------|------|
| 0 MVP | 3-4天 | 3-4天 |
| 1 健壮性 | 2-3天 | 5-7天 |
| 2 索引重建 | 2天 | 7-9天 |
| 3 Hilbert | 2-3天 | 9-12天 |
| 4 Python | 2天 | 11-14天 |
| 5 Node.js | 2天 | 13-16天 |
| 6 完善 | 2-3天 | 15-19天 |

**总计：15-19天**（比之前略有增加，但风险更低）

---

## 关键里程碑

| 时间 | 里程碑 | 可演示功能 |
|------|--------|-----------|
| 第3-4天 | **MVP完成** | Rust用户能创建1维聚簇表并排序 |
| 第7-9天 | **核心功能稳定** | 健壮性+索引重建完成，生产可用 |
| 第9-12天 | **多维支持** | 支持2-4维，功能完整 |
| 第11-14天 | **Python可用** | Python用户能使用聚簇功能 |
| 第15-19天 | **产品化** | 完整文档，性能基准，可发布 |

---

## 附录：关键设计决策回顾

| 决策点 | 选择 | 原因 |
|--------|------|------|
| 模块可见性 | 私有模块 | 封装内部实现，简化用户认知 |
| Hilbert 归一化 | 简单线性映射 | 与友商一致，避免复杂度 |
| 增量聚簇 | 预留接口，V1 报错 | 聚焦核心功能，降低复杂度 |
| 索引重建 | 强制自动重建 | 保证数据一致性 |
| 配置存储 | Schema Metadata | 利用现有机制，无需额外文件 |
| Fragment 级元信息 | 不维护 | 简化设计，依赖 min-max 统计 |

---

**设计文档完成，确认后进入实现阶段。**
